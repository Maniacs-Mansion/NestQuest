{
  "title": "NestQuest",
  "config": {
    "step": {
      "user": {
        "title": "Add NestQuest",
        "description": "Set up NestQuest to start coordinating the household. No additional configuration is required."
      }
    },
    "abort": {
      "single_instance_allowed": "NestQuest is already configured. Only a single instance is allowed."
    },
    "create_entry": {
      "default": "NestQuest was successfully configured."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "NestQuest Settings",
        "description": "Adjust how NestQuest plans, rolls over days, and idles its panel.",
        "data": {
          "horizon_days": "Generation horizon (days)",
          "day_rollover_time": "Day rollover time (HH:MM)",
          "panel_idle_timeout": "Panel idle timeout (seconds)",
          "admin_user_ids": "Admin users (parents)"
        }
      }
    },
    "errors": {
      "invalid": "Invalid value.",
      "invalid_time": "Time must use 24-hour HH:MM format.",
      "no_admins": "Select at least one admin user; an empty allowlist would lock everyone out.",
      "invalid_admin": "Invalid admin selection."
    }
  }
}
