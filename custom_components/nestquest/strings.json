{
  "title": "NestQuest",
  "config": {
    "step": {
      "user": {
        "title": "Add NestQuest",
        "description": "Set up NestQuest to start coordinating the household. No additional configuration is required."
      }
    },
    "abort": {
      "single_instance_allowed": "NestQuest is already configured. Only a single instance is allowed."
    },
    "create_entry": {
      "default": "NestQuest was successfully configured."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "NestQuest Settings",
        "description": "Adjust how the NestQuest API client polls and reaches the NestQuest API service.",
        "data": {
          "panel_idle_timeout": "Panel idle timeout (seconds)",
          "snapshot_staleness": "Last-good snapshot staleness (seconds)",
          "api_base_url": "NestQuest API base URL (http://host:port)",
          "panel_token": "Panel service token"
        }
      }
    },
    "errors": {
      "invalid": "Invalid value.",
      "invalid_url": "Enter the API base URL as a full http:// or https:// URL with a host.",
      "invalid_staleness": "Last-good snapshot staleness must be at least the update interval."
    }
  }
}
